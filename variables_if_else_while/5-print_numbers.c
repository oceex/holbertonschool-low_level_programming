# include <stdio.h>
/**
  * main - this is the starting main and its output the numbers
  * Description: this program should prints all the numbers with one digit
  * Return: 0
  */
int main(void)
{
int i;

for (i = 0; i <= 9; i++)
{
printf("%d", i);
}
printf("\n");
return (0);
}
