#include <stdio.h>
#include <stdlib.h>
#include "3-calc.h"

int main(int argc, char[] argv)
{
int i = 0, j = 0;
int value = atoi(argv[i][j]);
if (str[i][j] < '0' || argv[i][j] > '9')
{
printf("Error\n");
        return (98);
}
pr
return 0;
}
